<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- Custom styles -->
        <item name="android:textViewStyle">@style/AppTheme.Widget.TextView</item>
        <item name="editTextStyle">@style/AppTheme.Widget.EditText</item>
        <item name="imageButtonStyle">@style/AppTheme.Widget.ImageButton</item>
        <item name="spinnerStyle">@style/AppTheme.Widget.Spinner</item>
    </style>

    <!-- Theme for date picker dialog -->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <!--
        Text View Styles
    -->
    <style name="AppTheme.Widget.TextView" parent="TextAppearance.AppCompat">
        <item name="android:fontFamily">@font/nvm_font</item>
    </style>

    <style name="AppTheme.Widget.TextView.Label" parent="AppTheme.Widget.TextView">
        <item name="android:textColor">@color/darkGrey</item>
        <item name="android:textSize">@dimen/textsize_description</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.Widget.TextView.Error" parent="AppTheme.Widget.TextView.Label">
        <item name="android:textColor">@android:color/holo_red_light</item>
    </style>

    <!--
        Edit Text Styles
    -->
    <style name="AppTheme.Widget.EditText" parent="Widget.AppCompat.EditText">
        <item name="android:fontFamily">@font/nvm_font</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="backgroundTint">@color/lightGrey</item>
    </style>

    <!--
        Image Button Styles
    -->
    <style name="AppTheme.Widget.ImageButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:background">?attr/selectableItemBackgroundBorderless</item>
        <item name="android:padding">@dimen/margin_s</item>
    </style>

    <!--
        Spinner Styles
    -->
    <style name="AppTheme.Widget.Spinner" parent="Widget.AppCompat.Spinner">
        <item name="android:fontFamily">@font/nvm_font</item>
    </style>

</resources>
