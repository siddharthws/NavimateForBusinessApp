<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Signature Icon -->
    <com.biz.navimate.views.custom.NvmImageView
        android:id="@+id/iv_sign"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/ic_sign_grey"
        android:layout_gravity="center_vertical"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_gravity="center_vertical">

        <!-- Thumbnail of the signature currently set -->
        <com.biz.navimate.views.custom.PhotoThumbnailView
            android:id="@+id/pt_thumbnail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"/>

        <!-- Textbox for showing error -->
        <TextView
            android:id="@+id/tv_error"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Signature not set..."/>

    </LinearLayout>

    <!-- Clear Button -->
    <com.biz.navimate.views.custom.NvmImageButton
        android:id="@+id/ib_clear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_cross_grey"
        android:layout_gravity="center_vertical"
        android:visibility="gone"/>

    <!-- Edit Button -->
    <com.biz.navimate.views.custom.NvmImageButton
        android:id="@+id/ib_edit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_edit_grey"
        android:layout_gravity="center_vertical"/>

</merge>